# Memobase 全局配置文件
# 可以通过环境变量（如 MEMOBASE_LLM_API_KEY）覆盖对应的配置。

llm:
  provider: "openai"
  api_key: ""
  base_url: "https://api.openai.com/v1"
  model: "gpt-4o-mini"

embedding:
  provider: "openai"
  api_key: ""
  base_url: "https://api.openai.com/v1"
  model: "text-embedding-3-small"

vector_store:
  provider: "pgvector"
