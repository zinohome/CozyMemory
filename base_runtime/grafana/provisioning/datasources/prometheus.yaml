# Grafana 数据源自动配置 — 启动时 Grafana 自动注册这个 datasource
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
